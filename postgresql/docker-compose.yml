version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: dbpass

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

networks:
  default:
    external:
      name: proxy-network
