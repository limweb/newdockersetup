version: "3.8"

services:
  jenkins:
    image: yourdockeracc/jenkins-dind:latest
    user: root
    privileged: true
    ports:
      - "8080:8080"
      - "50000:50000"
    labels:
      - "virtual.host=your_jenkins.com" # your domain
      - "virtual.port=8080" # exposed port of this container
      - "virtual.tls-email=your@email.com" # ssl is now on
    environment:
      TZ: Asia/Bangkok
      VIRTUAL_HOST: "your_jenkins.com"
      VIRTUAL_PORT: "8080"
      LETSENCRYPT_HOST: "your_jenkins.com"
      LETSENCRYPT_EMAIL: "your@email.com"
    volumes:
      - "jenkins-data:/var/jenkins_home"
      - /var/run/docker.sock:/var/run/docker.sock
    logging:
      driver: "json-file" # ใช้ json-file driver (default)
      options:
        max-size: "10m" # จำกัดขนาดของแต่ละไฟล์ log ไม่ให้เกิน 10MB
        max-file: "5" # เก็บไฟล์ log ไว้สูงสุด 5 ไฟล์

volumes:
  #ถ้ามีการใช้ docker-compose ซ้ำ volumes ต้องไม่ซ้ำ
  # โดยเฉพาะ database   เช่น postgresql  vol เป็น sqldata
  #ถ้าซ้ำจะทำให้ ข้อมูลถูกใช้ทับกันครับ ต้อง เปลียนเป้น sqldata2 เป็นต้น
  jenkins-data:

networks:
  default:
    external: true
    name: proxy-network
